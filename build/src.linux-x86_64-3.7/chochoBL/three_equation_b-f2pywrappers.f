C     -*- fortran -*-
C     This file is autogenerated with f2py (version:2)
C     It contains Fortran 77 wrappers to fortran functions.

      subroutine f2pywrappopbit (popbitf2pywrap)
      external popbit
      logical popbitf2pywrap, popbit
      popbitf2pywrap = .not.(.not.popbit())
      end


      subroutine f2pyinitadi4fbuf(setupfunc)
      external setupfunc
      integer*4 adi4buf(512)
      integer adi4ibuf
      common /adi4fbuf/ adi4buf,adi4ibuf
      call setupfunc(adi4buf,adi4ibuf)
      end

      subroutine f2pyinitadi8fbuf(setupfunc)
      external setupfunc
      integer*8 adi8buf(512)
      integer adi8ibuf
      common /adi8fbuf/ adi8buf,adi8ibuf
      call setupfunc(adi8buf,adi8ibuf)
      end

      subroutine f2pyinitadr4fbuf(setupfunc)
      external setupfunc
      real*4 adr4buf(512)
      integer adr4ibuf
      common /adr4fbuf/ adr4buf,adr4ibuf
      call setupfunc(adr4buf,adr4ibuf)
      end

      subroutine f2pyinitadr8fbuf(setupfunc)
      external setupfunc
      real*8 adr8buf(512)
      integer adr8ibuf
      common /adr8fbuf/ adr8buf,adr8ibuf
      call setupfunc(adr8buf,adr8ibuf)
      end

      subroutine f2pyinitadc8fbuf(setupfunc)
      external setupfunc
      complex*8 adc8buf(512)
      integer adc8ibuf
      common /adc8fbuf/ adc8buf,adc8ibuf
      call setupfunc(adc8buf,adc8ibuf)
      end

      subroutine f2pyinitadc16fbuf(setupfunc)
      external setupfunc
      complex*16 adc16buf(512)
      integer adc16ibuf
      common /adc16fbuf/ adc16buf,adc16ibuf
      call setupfunc(adc16buf,adc16ibuf)
      end

      subroutine f2pyinitads1fbuf(setupfunc)
      external setupfunc
      character ads1buf(512)
      integer ads1ibuf
      common /ads1fbuf/ ads1buf,ads1ibuf
      call setupfunc(ads1buf,ads1ibuf)
      end

      subroutine f2pyinitadbitfbuf(setupfunc)
      external setupfunc
      integer*4 adbitbuf
      integer adbitibuf
      common /adbitfbuf/ adbitbuf,adbitibuf
      call setupfunc(adbitbuf,adbitibuf)
      end

      subroutine f2pyinitallbufferrepeats(setupfunc)
      external setupfunc
      integer indexi4repeats(5)
      integer indexi8repeats(5)
      integer indexr4repeats(5)
      integer indexr8repeats(5)
      integer indexc8repeats(5)
      integer indexc16repeats(5)
      integer indexs1repeats(5)
      integer indexbitrepeats(5)
      integer indexptrrepeats(5)
      integer nbbufrepeat
      common /allbufferrepeats/ indexi4repeats,indexi8repeats,inde
     &xr4repeats,indexr8repeats,indexc8repeats,indexc16repeats,indexs1re
     &peats,indexbitrepeats,indexptrrepeats,nbbufrepeat
      call setupfunc(indexi4repeats,indexi8repeats,indexr4repeats,
     &indexr8repeats,indexc8repeats,indexc16repeats,indexs1repeats,index
     &bitrepeats,indexptrrepeats,nbbufrepeat)
      end

      subroutine f2pyinitbuftraffic(setupfunc)
      external setupfunc
      integer*8 buffertraffic
      common /buftraffic/ buffertraffic
      call setupfunc(buffertraffic)
      end


